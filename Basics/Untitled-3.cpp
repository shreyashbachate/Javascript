#include <cctype>
#include<fstream>

using namespace std;

int main()

{

ifstream ifile;

//content of file: geeksfor geeks

char last:

ifile.open ("text.txt""); ifile.ignore (256, "); last = ifile.get(): cout <<""Your initial is ""<<last << '\n'; ifile.closet(); return0;

}
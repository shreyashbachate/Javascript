var isLoggedIn = true;

var emailVerified = true;

var cardValid = true;

if (isLoggedIn && emailVerified && cardValid) {
    console.log("You can purchase");
} else {
    console.log("You are not purchase");
}